# ==============================================================================
# 환경 변수 예시 파일
#
# - 필요한 환경에 맞춰 이 파일을 .env.local, .env.dev, .env.prod로 복사한다.
# - 실제 값은 복사한 파일에만 입력한다.
# - 운영 시크릿, 접근 키, 비밀번호는 이 예시 파일에 적지 않는다.
#
# .env.dev 값 기준
# - 개발 서버에서 Compose를 실행할 때는 컨테이너 이름을 호스트로 사용한다.
# - 개발자 PC에서 `make be-dev-up`을 실행할 때는 개발 서버 주소나 터널 주소를 사용한다.
# ==============================================================================

# ------------------------------------------------------------------------------
# Spring Boot
# ------------------------------------------------------------------------------
SPRING_PROFILES_ACTIVE=
SERVER_PORT=

# ------------------------------------------------------------------------------
# PostgreSQL / PostGIS
# ------------------------------------------------------------------------------
POSTGRES_DB=
POSTGRES_PORT=
DB_URL=
DB_USERNAME=
DB_PASSWORD=
JPA_DDL_AUTO=

# ------------------------------------------------------------------------------
# Redis / ElastiCache
# ------------------------------------------------------------------------------
REDIS_HOST=
REDIS_PORT=

# ------------------------------------------------------------------------------
# MinIO / S3
# ------------------------------------------------------------------------------
MINIO_API_PORT=
MINIO_CONSOLE_PORT=
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=

# ------------------------------------------------------------------------------
# GraphHopper
# ------------------------------------------------------------------------------
GRAPHHOPPER_PORT=
GRAPHHOPPER_ADMIN_PORT=
GRAPHHOPPER_DATA_DIR=
GRAPHHOPPER_DATA_FILE=
GRAPHHOPPER_GRAPH_LOCATION=
GRAPHHOPPER_BASE_URL=
