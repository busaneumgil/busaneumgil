# 기능명세서

> 작성일: 2026-04-14
> 최종 개정일: 2026-04-29
> 기준 문서: `FE/docs/2026-04-27_온보딩_디자인_화면_구성_분석.md`, `FE/docs/2026-04-27_로그인_필수_전환_FE_정합성_및_구현_영향.md`, `FE/docs/2026-04-27_FE_Docs_문서_정합성_재검토.md`, `Docs/기획/2026-04-09_화면명세서.md`, `Docs/기획/2026-04-11_MVP_화면명세서.md`, `Docs/API/2026-04-12_API_전체_목록.md`, `FE/docs/2026-04-27_BE_연동_작업_요청사항.md`
> 대상 플랫폼: Android 모바일 앱

---

## 0. 정렬 기준

| 항목 | 최신 기준 |
| --- | --- |
| 앱 진입 gate | 인증 세션이 없으면 `AUTH-001`로 이동한다. 기존 회원은 `authSession -> selectedPrimaryUserType 홈 분기`로 이동하고, 신규 사용자는 소셜 인증 후 `signupToken`으로 온보딩을 완료한 뒤 회원가입을 생성한다. |
| 로그인 정책 | `AUTH-001`은 필수 로그인 화면이며 카카오/네이버/구글 소셜 로그인만 제공한다. 운영 기준 `비로그인으로 계속`, guest mode, 비로그인 핵심 기능 사용은 제거한다. |
| 온보딩 정책 | 온보딩은 `ONB-001 사용자 유형 1차 선택` -> `ONB-002 저시력자 전용 후속 화면` 또는 `ONB-003 보행약자 2차 선택` -> `ONB-004 약관/동의` -> `OS 위치 권한 시스템 팝업` -> `ONB-005 튜토리얼` 5단계다. 필수 약관 동의와 사용자 유형을 확인한 뒤 `POST /auth/signup`으로 회원가입을 완료한다. `users`에는 1차 사용자 유형과 조건부 보행약자 세부 유형만 저장한다. |
| 사용자 용어 | 온보딩 1차 분기 용어는 `저시력자`, `보행약자`로 통일한다. 보행약자 2차 선택지는 `전동휠체어 사용자`, `수동휠체어 사용자`, `기타 보행약자`다. |
| 약관/권한 구분 | `개인위치정보 수집·이용 동의`는 `ONB-004`의 법적 동의 항목이고, `OS 위치 권한 허용`은 `ONB-004` 다음 단계의 시스템 팝업이다. |
| 하단 탭 | 보행약자 기본 하단 탭은 `홈 / 북마크 / 제보 / 마이페이지` 4탭이다. 저시력자 전용 UI는 별도 `LV-*` 화면군에서 `홈 / 북마크 / 카테고리 / 마이페이지`를 사용한다. |
| 지도 필터 | 빠른 필터는 `장애인 화장실`, `엘리베이터`, `전동보장구 충전 가능` 접근성 속성이다. 더보기는 장소 카테고리 7개를 노출한다. |
| 경로 구조 | `ROUTE-01`은 경로 지도/탐색 결과, `ROUTE-02`는 경로 상세, `NAV-01`은 길안내 진행 화면이다. |
| 경로 옵션 | `안전한 길`, `최단거리` 2개만 사용한다. |
| 북마크 명칭 | 사용자 노출 명칭은 `북마크`로 통일한다. `BMK-01`은 장소 탭, `BMK-02`는 경로 탭이다. |
| 제보 구조 | 제보는 `REPORT-01`~`REPORT-04` 4단계 플로우다. |
| 데이터 책임 | 서버 데이터가 기준이며 로컬은 `cache/offline/pending` 보조 역할로만 사용한다. `local-first`를 production 핵심 정책으로 서술하지 않는다. |

---

## 1. 인증 및 온보딩

| 화면 ID | 핵심 기능 | 주요 전환 규칙 | 데이터/정책 |
| --- | --- | --- | --- |
| `AUTH-001` | 소셜 로그인으로 기존 회원 세션 또는 신규 사용자 `signupToken`을 받는다. | 기존 회원은 사용자 유형에 따라 홈으로 이동하고, 신규 사용자는 `ONB-001`로 이동한다. | 카카오/네이버/구글만 제공한다. guest mode와 비로그인 우회는 제공하지 않는다. |
| `ONB-001` | 신규 사용자의 사용자 유형 1차 선택을 받는다. | `저시력자` 선택 시 `ONB-002`, `보행약자` 선택 시 `ONB-003`으로 이동한다. | 소셜 인증 후 받은 `signupToken`이 있을 때 진입한다. |
| `ONB-002` | 저시력자 전용 후속 화면을 제공한다. | 후속 입력 완료 후 `ONB-004`로 이동한다. | 세부 선택 요소는 문서 근거가 있는 범위만 반영하며, 현재는 화면 존재와 연결을 우선 확정한다. |
| `ONB-003` | 보행약자 2차 선택을 받는다. | 사용자 유형 선택 후 `다음`으로 `ONB-004`로 이동한다. | 선택지는 `전동휠체어 사용자`, `수동휠체어 사용자`, `기타 보행약자`만 사용한다. `장애등급`은 이 단계에 포함하지 않는다. 서버에는 `selectedMobilitySubtype` 한 개 값으로 저장한다. |
| `ONB-004` | 필수 약관/동의를 수집하고 전문 확인 동선을 제공한다. | 필수 동의 완료 후 `다음` 선택 시 `OS 위치 권한 시스템 팝업`을 띄우고, 응답 이후 `ONB-005`로 이동한다. | 법적 동의 항목은 `전체 동의`, `[필수] 서비스 이용약관 및 위치기반서비스 이용약관 동의`, `[필수] 민감정보 수집·이용 동의`, `[필수] 개인위치정보 수집·이용 동의`, `[필수] 만 14세 이상입니다`, `[확인] 개인정보 처리방침 확인`이다. 서버에는 별도 약관 동의 필드를 저장하지 않고 가입 완료 조건으로만 확인한다. |
| `ONB-005` | 위치 권한 응답 이후 튜토리얼을 제공하고 온보딩을 완료한다. | 튜토리얼 완료 시 `POST /auth/signup`을 호출하고, 성공 후 `저시력자`는 `LV-HOM-01`, `보행약자`는 `MAP-01`로 이동한다. | 튜토리얼은 홈 진입 전 마지막 단계다. |

### 1.1 온보딩 공통 규칙

| 항목 | 기준 |
| --- | --- |
| 단계 수 | `ONB-001`~`ONB-005` 5단계로 고정한다. |
| 용어 | 온보딩 1차 분기는 `저시력자`, `보행약자`로 통일하고, 보행약자 2차 선택은 `전동휠체어 사용자`, `수동휠체어 사용자`, `기타 보행약자`로 통일한다. |
| 약관/권한 | `ONB-004`의 법적 동의와 그 다음 단계의 `OS 위치 권한 시스템 팝업`은 분리해 서술한다. |
| 상태 표현 | 선택, 오류, 비활성, 완료 상태는 색상만이 아니라 텍스트와 semantics를 함께 제공한다. |
| 재진입 | `MYP-01`의 `사용자 유형 변경`은 `ONB-001`부터 다시 시작한다. 보행약자 2차 선택은 `PATCH /users/me/user-type`으로 `selectedMobilitySubtype`에 저장한다. |

### 1.2 저시력자 전용 음성 중심 UI

| 화면 ID | 역할 | 주요 전환 |
| --- | --- | --- |
| `LV-ONB-01` | 저시력자 약관 동의 | `LV-ONB-02` |
| `LV-ONB-02` | 저시력자 민감정보 동의 | `LV-ONB-03` |
| `LV-ONB-03` | 저시력자 개인위치정보 동의 | `LV-ONB-04` |
| `LV-ONB-04` | 저시력자 만 14세 이상 확인 | `LV-ONB-05` |
| `LV-ONB-05` | 저시력자 개인정보 처리방침 확인 | OS 위치 권한 시스템 팝업 -> `ONB-005` |
| `LV-HOM-01` | 저시력자 홈 | `LV-SEA-01`, `LV-BMK-01`, `LV-CAT-01`, `LV-MYP-01` |
| `LV-SEA-01` | 저시력자 음성 입력 진행 | `LV-RES-01`, `LV-HOM-01` |
| `LV-RES-01` | 저시력자 장소 결과 목록 | `LV-NAV-01`, `LV-HOM-01` |
| `LV-CAT-01` | 저시력자 카테고리 | `LV-RES-01`, `LV-HOM-01` |
| `LV-BMK-01` | 저시력자 북마크 | `LV-BRF-01`, `LV-NAV-01`, `LV-HOM-01` |
| `LV-BRF-01` | 저시력자 경로 브리핑 | `LV-NAV-01`, `LV-BMK-01` |
| `LV-NAV-01` | 저시력자 길안내 진행 | `LV-ARR-01`, `LV-HOM-01` |
| `LV-ARR-01` | 저시력자 도착 완료 | `LV-HOM-01` |
| `LV-MYP-01` | 저시력자 마이페이지 | `ONB-001`, `AUTH-001`, 앱 정보 상세 |

| 규칙 | 기준 |
| --- | --- |
| 별도 화면 원칙 | 저시력자 UI는 공통 `MAP-01`, `DEST-02`, `ROUTE-02`, `NAV-01`, `BMK-01`, `MYP-01`의 모드 예외가 아니라 별도 `LV-*` 화면군으로 다룬다. |
| 동의 방식 | `LV-ONB-01`~`LV-ONB-05`는 각 화면 중앙 대형 버튼 더블탭으로 다음 화면으로 이동한다. |
| 위치 권한 시점 | `LV-ONB-05` 완료 후 `OS 위치 권한 시스템 팝업`을 띄운다. 법적 동의와 시스템 권한은 분리 유지한다. |
| 튜토리얼 연결 | OS 위치 권한 응답 후 `ONB-005`를 거쳐 `LV-HOM-01`로 진입한다. |

---

## 2. 지도 홈, 시설, 목적지 설정

| 화면 ID | 핵심 기능 | 주요 전환 규칙 | 데이터/정책 |
| --- | --- | --- | --- |
| `MAP-01` | 지도 홈에서 검색, 시설 탐색, 최근 목적지, 하단 4탭 진입점을 제공한다. | 검색바는 `DEST-01`, 시설 마커는 `FAC-01`, 북마크 탭은 `BMK-01`, 제보 탭은 `REPORT-01`, 마이페이지 탭은 `MYP-01`로 이동한다. | 홈 탭 활성 상태다. 위치 권한이 없으면 기능 제한 안내를 제공한다. |
| `FAC-01` | 시설 상세 바텀시트에서 시설 정보, 접근성 라벨, 북마크, 길찾기를 제공한다. | `길찾기` 선택 시 `DEST-03` 확인 후 `ROUTE-01`로 연결한다. | 지도 또는 북마크 장소 상세에서 진입한다. 장소 식별자는 내부 `placeId`를 기준으로 하며, 카카오 검색 결과는 `provider=KAKAO`, `providerPlaceId`로 조회/생성한다. |
| `DEST-01` | 목적지 검색과 최근 방문지 목록을 제공한다. | 검색 실행 시 `DEST-02`, 최근 방문지 선택 시 `DEST-03`, `전체보기` 선택 시 `BMK-01`로 이동한다. | 하단 탭은 유지한다. 마이크 버튼은 STT 장소 검색 진입점이다. |
| `DEST-02` | 검색 결과를 카드 목록으로 보여주고 후보를 고르게 한다. | 결과 카드 선택 시 항상 `DEST-03`으로 이동한다. | 검색 결과에서 곧바로 `ROUTE-01`에 진입하지 않는다. |
| `DEST-03` | 지도와 목적지 바텀시트로 도착지를 최종 확인한다. | `도착지로 설정` 선택 시 설문 필요 여부에 따라 `DEST-04` 또는 `ROUTE-01`로 이동한다. | 장소 식별자, 좌표, 주소를 일관되게 유지해야 한다. |
| `DEST-04` | 첫 사용 시 현재 이동 상태 설문을 제공한다. | 상태 선택 후 `ROUTE-01`로 이동한다. | 표시 조건, 저장 위치, 온보딩 사용자 유형과의 우선순위는 미정이다. |

### 2.1 지도 및 검색 공통 규칙

| 항목 | 기준 |
| --- | --- |
| 접근성 필터 | `MAP-01` 빠른 필터는 `장애인 화장실`, `엘리베이터`, `전동보장구 충전 가능`이다. 더보기는 음식·카페, 관광지, 숙박, 의료·보건, 복지·돌봄, 공공기관, 기타 카테고리를 제공한다. |
| 최근 목적지 | 별도 전체 목록 화면을 만들지 않고 `BMK-01`로 연결한다. |
| STT | STT 처리 주체와 서버 연동 방식은 `BE 확인 필요`다. |
| 장소 데이터 | 검색 결과와 시설 상세는 장소 식별자, 좌표, 주소를 일관되게 내려줘야 한다. |

### 2.2 저시력자 전용 별도 화면군 지도/검색 규칙

| 항목 | 기준 |
| --- | --- |
| 홈 화면 | 저시력자 전용 지도 홈은 `MAP-01`이 아니라 `LV-HOM-01`이다. |
| 홈 기본 CTA | `LV-HOM-01`은 `음성 입력`, `현재 위치` 2개 대형 버튼을 우선 노출한다. |
| 하단 탭 | `LV-HOM-01`은 `홈 / 북마크 / 카테고리 / 마이페이지`를 사용한다. |
| 카테고리 구성 | `LV-CAT-01`은 `화장실`, `음식점`, `숙박시설`, `병원` 4개 카드로 구성한다. |
| 결과 화면 | 검색 결과와 카테고리 결과는 모두 `LV-RES-01`을 사용한다. 첫 화면에서는 2개 카드만 크게 보여주고 스크롤로 추가 결과를 본다. |
| 결과 카드 CTA | `LV-RES-01` 카드마다 `북마크`, `길찾기`를 제공한다. |
| 길찾기 경로 | `LV-RES-01`의 `길찾기`는 `LV-NAV-01` 즉시 진입을 기준으로 한다. |

---

## 3. 경로 탐색, 길안내, 도착, 평가

| 화면 ID | 핵심 기능 | 주요 전환 규칙 | 데이터/정책 |
| --- | --- | --- | --- |
| `ROUTE-01` | 경로 지도와 탐색 결과를 보여주고 경로 옵션을 비교한다. | 옵션 카드 본문 선택은 선택 상태만 변경한다. 카드 우측 상단 화살표는 `ROUTE-02`, `길 안내 시작`은 `NAV-01`로 이동한다. | `안전한 길`, `최단거리` 2개 옵션만 사용한다. 하단 탭은 숨긴다. |
| `ROUTE-02` | 선택한 경로의 단계별 상세와 주의 구간을 보여준다. | `길 안내 시작` 선택 시 `NAV-01`로 이동하고, 뒤로가기는 `ROUTE-01`로 복귀한다. | 엘리베이터, 경사, 단차, 횡단보도 같은 상세 라벨은 텍스트로 함께 제공한다. |
| `NAV-01` | 실시간 길안내 진행 상태와 다음 행동, 남은 거리/시간을 제공한다. | 상세 정보는 `ROUTE-02`, 도착 완료는 `ARR-01`로 이동한다. | 경로 이탈 시 재검색/재탐색으로 정리한다. 중도 종료 후 이동 정책은 미정이다. |
| `ARR-01` | 도착 완료를 알리고 후속 행동을 제공한다. | `홈으로 돌아가기`는 `MAP-01`, `새 경로 탐색하기`는 `DEST-01`로 이동한다. 진입 즉시 `ARR-02`를 자동 표시한다. | 홈 탭이 활성인 완료 화면이다. |
| `ARR-02` | 경로 만족도 평가와 경로 저장을 수집한다. | `평가 등록`은 `POST /route-ratings`, `이 경로 저장하기`는 경로 북마크 저장, `X`는 평가 건너뛰기다. | 평가는 방금 안내받은 경로에 별점만 저장한다. 경로 저장은 출발지/도착지/옵션만 저장한다. |

### 3.1 경로 공통 규칙

| 항목 | 기준 |
| --- | --- |
| 경로 분리 | `ROUTE-01`, `ROUTE-02`, `NAV-01`은 서로 다른 책임을 가진다. |
| 경로 옵션 | 사용자 노출 경로 옵션은 `안전한 길`, `최단거리` 2개만 허용한다. |
| 길안내 집중 | `NAV-01`에서는 하단 탭을 숨긴다. |
| 평가 표시 시점 | `ARR-01` 진입 즉시 `ARR-02`를 표시한다. |
| 평가 저장 범위 | 별점만 저장하고 의견 텍스트는 저장하지 않는다. |

### 3.2 저시력자 전용 별도 화면군 경로/도착 규칙

| 항목 | 기준 |
| --- | --- |
| 브리핑 화면 | 저시력자 경로 브리핑은 `ROUTE-02`가 아니라 `LV-BRF-01`이다. |
| 경로 브리핑 구성 | `LV-BRF-01`은 단계 요약 3개 내외, 음성 재생 버튼, 대형 `시작/중지` 버튼을 제공한다. |
| 안내 화면 | 저시력자 길안내 진행은 `NAV-01`이 아니라 `LV-NAV-01`이다. |
| 안내 정보 우선순위 | `LV-NAV-01`은 `남은 거리`, `남은 시간`, `현재 위치`, `안내 종료`를 대형 정보/버튼으로 우선 배치한다. |
| 도착 화면 | 저시력자 도착 완료는 `ARR-01`이 아니라 `LV-ARR-01`이다. |
| 도착 CTA | `LV-ARR-01`은 `도착지 저장`, `완료`를 우선 CTA로 사용한다. `완료`는 `LV-HOM-01` 복귀다. |

---

## 4. 북마크

| 화면 ID | 핵심 기능 | 주요 전환 규칙 | 데이터/정책 |
| --- | --- | --- | --- |
| `BMK-01` | 장소 북마크 목록을 조회하고 상세/길찾기/삭제를 제공한다. | 카드 본문 선택 시 `FAC-01`, `길찾기` 선택 시 `DEST-03` 확인 후 `ROUTE-01`, 탭 전환 시 `BMK-02`로 이동한다. | 사용자 노출 명칭은 `북마크`다. 편집 모드는 삭제만 제공한다. |
| `BMK-02` | 경로 북마크 목록을 조회하고 재탐색/삭제를 제공한다. | 카드 본문 또는 `길찾기` 선택 시 저장된 출발지/도착지/옵션으로 다시 경로를 탐색한다. 탭 전환 시 `BMK-01`로 이동한다. | 실제 경로 상세는 저장하지 않는다. 정렬, 전체 선택, 다중 선택은 제공하지 않는다. |

### 4.1 북마크 공통 규칙

| 항목 | 기준 |
| --- | --- |
| 명칭 | `저장`, `Saved` 대신 `북마크`를 사용한다. |
| 데이터 책임 | 북마크 장소와 북마크 경로 모두 서버 데이터가 기준이다. 로컬은 `cache/offline/pending` 보조 역할이다. |
| 상세 연결 | 카드 본문은 상세, CTA는 길찾기 역할을 분리한다. |
| 경로 저장 범위 | 경로 북마크는 출발지, 도착지, 경로 옵션만 저장한다. |

### 4.2 저시력자 전용 별도 화면군 북마크 규칙

| 항목 | 기준 |
| --- | --- |
| 북마크 화면 | 저시력자 북마크는 `BMK-01`이 아니라 `LV-BMK-01`이다. |
| 리스트 패턴 | `LV-BMK-01`은 검색 결과와 같은 2카드 패턴을 사용한다. |
| 카드 CTA | 각 카드에 `북마크(해제)`, `길찾기`를 제공한다. |
| 본문 제스처 | 카드의 빈 영역 더블탭은 `LV-BRF-01` 경로 브리핑으로 연결한다. |
| 음성 안내 연결 | `LV-BRF-01`의 음성 재생 버튼으로 경로를 음성으로 들을 수 있게 한다. |

---

## 5. 제보

| 화면 ID | 핵심 기능 | 주요 전환 규칙 | 데이터/정책 |
| --- | --- | --- | --- |
| `REPORT-01` | 제보할 장애물 유형을 선택한다. | 유형 선택 후 `REPORT-02`로 이동한다. | 유형은 `계단/단차가 없다고 나와있는데 실제로 있음`, `점자블록`, `인도라고 적혀 있는데 실제로 없음`, `경사로`, `인도폭` 5개다. |
| `REPORT-02` | 제보 위치를 확인하고 조정한다. | 위치 확정 후 `REPORT-03`으로 이동한다. | 위치 확정 방식은 pin drag다. |
| `REPORT-03` | 사진과 설명을 입력하고 제보를 제출한다. | 제출 성공 시 `REPORT-04`로 이동한다. | 사진은 선택 입력이며 최대 5장이다. image upload API와 URL 정책은 별도 확인이 필요하다. |
| `REPORT-04` | 제보 접수 완료를 안내한다. | `제보 내역 확인하기`는 `MYP-02`, `홈으로 이동`은 `MAP-01`로 연결한다. | 제보 처리 상태는 사용자 화면에 노출하지 않는다. |

### 5.1 제보 공통 규칙

| 항목 | 기준 |
| --- | --- |
| 인증/계정 연결 | 제보는 사용자 계정과 연결한다. 익명 제보를 기본 정책으로 사용하지 않는다. |
| 데이터 책임 | 제출 이후 제보 이력은 서버 데이터가 기준이다. 작성 중 draft만 로컬에서 임시 보관할 수 있다. |
| 상태 노출 | 운영 내부 상태가 존재하더라도 사용자 화면에는 노출하지 않는다. |
| 목록/상세 | 목록은 최신순이며 대표 사진 1장만 표시한다. 상세에서 전체 사진을 보여준다. |

---

## 6. 마이페이지

| 화면 ID | 핵심 기능 | 주요 전환 규칙 | 데이터/정책 |
| --- | --- | --- | --- |
| `MYP-01` | 소셜 이름/닉네임, 사용자 유형 요약, 활동 배지, 사용자 유형 변경, 제보 내역, 앱 정보 및 도움말, 로그아웃을 제공한다. | `사용자 유형 변경`은 `ONB-001`, `제보 내역`은 `MYP-02`, `앱 정보 및 도움말`은 `MYP-03`, 로그아웃 완료 시 `AUTH-001`로 이동한다. | 개인정보 항목은 노출하지 않는다. 활동 배지 산정은 미정이다. |
| `MYP-02` | 사용자 제보 목록을 제공한다. | 뒤로가기는 `MYP-01`로 복귀한다. | 최신순 목록, 대표 사진 1장, 처리 상태 미노출을 기준으로 한다. |
| `MYP-03` | 이용 가이드, 약관/개인정보, 고객센터/1:1 문의 placeholder, 회원탈퇴 동선을 제공한다. | `회원탈퇴` 처리 완료 시 `AUTH-001`로 이동한다. | 고객센터/1:1 문의는 `준비중인 기능입니다.` 메시지로 처리한다. 개인정보처리방침, 이용가이드, 이용약관은 노션으로 관리한다. |

### 6.1 계정 공통 규칙

| 항목 | 기준 |
| --- | --- |
| 로그아웃 | 앱 세션 토큰을 삭제하고 서버의 온보딩 상태는 유지한다. 확인 다이얼로그는 UX에서 결정한다. |
| 회원탈퇴 | 재인증이 필요하며 처리 후 로그인 화면으로 이동한다. 북마크, 경로 저장, 별점 평가는 삭제하고 제보 내역은 보관한다. |
| 이용 가이드 | 처음 튜토리얼 화면을 재사용할 수 있다. |

### 6.2 저시력자 전용 별도 화면군 마이페이지 규칙

| 항목 | 기준 |
| --- | --- |
| 마이페이지 화면 | 저시력자 전용 마이페이지는 `MYP-01`이 아니라 `LV-MYP-01`이다. |
| 메뉴 구조 | `LV-MYP-01`은 `모드변경`, `로그아웃`, `앱정보` 3개 버튼 중심 구조를 우선한다. |
| 표시 우선순위 | 프로필 배지, 제보 내역, 부가 메뉴는 저시력자 전용 1차 UI에서 후순위로 둔다. |
| 모드변경 동선 | `모드변경`은 `ONB-001` 재진입 또는 전용 모드 선택 화면으로 연결한다. 최종 구현 경로는 추가 확인이 필요하다. |

---

## 7. 데이터 책임 및 저장 위치

| 데이터 영역 | 기준 저장소 | 로컬 역할 | 비고 |
| --- | --- | --- | --- |
| 인증 세션 | 서버 인증 응답 기준 | secure storage/token cache | 로그아웃 시 앱 세션 토큰을 삭제한다. |
| 회원가입 완료 상태 | 서버 `users` row 존재 여부 기준 | 온보딩 재개용 임시 보조 상태 | 단계별 완료 상태와 별도 완료 boolean은 서버에 저장하지 않는다. |
| `selectedPrimaryUserType` | 서버 `users.selectedPrimaryUserType` 기준 | 현재 세션 선택 상태 보조 | `LOW_VISION`, `MOBILITY_IMPAIRED` 중 하나로 저장한다. |
| `selectedMobilitySubtype` | 서버 `users.selectedMobilitySubtype` 기준 | 현재 세션 선택 상태 보조 | 보행약자 2차 선택 값이다. `selectedPrimaryUserType=LOW_VISION`이면 `null`, `MOBILITY_IMPAIRED`이면 `POWER_WHEELCHAIR`, `MANUAL_WHEELCHAIR`, `OTHER_MOBILITY` 중 하나로 저장한다. |
| `locationPermissionStatus` | 시스템 권한 응답 기준 | 현재 세션 캐시/재시도 상태 | `ONB-004` 다음 단계의 `OS 위치 권한 시스템 팝업` 결과를 기록한다. |
| 장소/경로 검색 결과 | 서버 및 외부 연동 응답 기준 | 현재 세션 캐시 | 장소 식별자, 좌표, 주소 일관성 필요 |
| 북마크 장소 | 서버 북마크 데이터 기준 | `cache/offline/pending` | 사용자 노출 용어는 `북마크`로만 사용 |
| 북마크 경로 | 서버 북마크 경로 데이터 기준 | `cache/offline/pending` | 출발지/도착지/옵션만 저장한다. |
| 제보 draft | 제출 전 로컬 임시 상태 | 작성 중 임시 저장 | 제출 후에는 서버 제보 데이터가 기준 |
| 제보 이력 | 서버 사용자 제보 데이터 기준 | 목록 캐시 | 처리 상태는 FE 미노출 |
| 경로 평가 | 서버 `route_ratings` 기준 | 제출 전 UI 임시 상태 | 방금 안내받은 경로에 별점만 저장한다. |

---

## 8. MVP 제외 또는 후순위

- `SCR-001`~`SCR-003`, `ONB-01`~`ONB-03` 같은 구 온보딩 구조
- 운영 기준 `비로그인으로 계속`, guest mode, 비로그인 지도/경로/북마크/제보 진입
- 3탭 또는 5탭 후보 구조
- 저시력자 전용 후속 화면을 별도 `VIS` 메인 플로우로 대체해 서술하는 정책
- 사용자 노출 명칭으로 `저장`, `Saved`, `자주 가는 길`을 기본 화면명처럼 사용하는 정책
- 북마크 정렬, 전체 선택, 다중 선택
- 제보 처리 상태 화면
- 고객센터/1:1 문의 실제 연결
- 길안내 중도 종료 정책 확정
- `local-first`나 로컬 source of truth를 production 핵심 정책으로 서술하는 방식

---

## 9. 미확정 항목

| 영역 | 내용 |
| --- | --- |
| 로그인 UX 순서 | 기존 회원은 `authSession -> selectedPrimaryUserType 홈 분기`로 이동한다. 신규 사용자는 소셜 인증 후 `signupToken`으로 `ONB-001 사용자 유형 1차 선택`에 진입하고, 온보딩 완료 시 `POST /auth/signup` 성공 후 홈으로 이동한다. |
| 권한 | `ONB-004`에서 사용자가 `다음`을 누른 뒤 다음 단계에서 `OS 위치 권한 시스템 팝업`을 띄우는 것으로 고정한다. |
| 저시력자 후속 화면 | `ONB-002`의 상세 입력 요소와 저장 스키마는 추가 확인이 필요하다. 현재는 화면 존재와 흐름 연결까지만 확정한다. |
| 저시력자 전용 상세 | `LV-MYP-01`의 앱 정보 상세를 기존 `MYP-03` 재사용으로 볼지 별도 `LV-*`로 분리할지는 추가 확인이 필요하다. |
| 목적지 설문 | `DEST-04` 노출 조건, 저장 위치, 온보딩 사용자 유형과의 우선순위가 미정이다. |
| 길안내 종료 | `NAV-01` 중도 종료 후 `MAP-01`, `ROUTE-01`, 이전 화면 중 어디로 이동할지 미정이다. |
| 경로 평가 | 평가 실패 처리와 중복 평가 허용 여부는 미정이다. |
| 프로필 매핑 | 개인정보는 노출하지 않는다. 소셜 이름/닉네임, 1차 사용자 유형, 보행약자 세부 유형 표시 문구만 UX 확정이 필요하다. |
| 활동 배지 | `제보왕`, `도움왕`, `참여왕` 산정 기준이 미정이다. |
| 알림 | 푸시/진동 설정은 서버 저장 범위에서 제외한다. 앱 내부 설정 노출 여부만 UX 확정이 필요하다. |
| 로그아웃/탈퇴 | 회원탈퇴 재인증 방식은 미정이다. 탈퇴 시 북마크/경로 저장/별점은 삭제하고 제보 내역은 보관한다. |
