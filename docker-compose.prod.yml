name: s14p31e102-prod

# Production compose skeleton.
# Only place EC2-hosted runtime services here.
#
# Expected candidates:
# - backend (Spring Boot)
# - ai (Python Flask intent service)
# - graphhopper-runtime (disabled by default until graph-cache artifact flow is ready)
#
# Do NOT place managed AWS resources here:
# - RDS
# - ElastiCache
# - S3
#
# Do NOT place S1 operations tools here:
# - Jenkins
# - Grafana / Portainer / SonarQube / PLG
#
# Use with:
#   docker compose --env-file .env.prod -f docker-compose.prod.yml up -d
#
# GraphHopper prod runtime is intentionally profile-gated.
# Enable it only after the PostgreSQL LineString -> graph-cache build/deploy flow is ready:
#   docker compose --env-file .env.prod -f docker-compose.prod.yml --profile graphhopper up -d

services:
  graphhopper:
    profiles:
      - graphhopper
    build:
      context: .
      dockerfile: INF/graphhopper/Dockerfile
    restart: unless-stopped
    environment:
      GRAPHHOPPER_DATA_FILE: ${GRAPHHOPPER_DATA_FILE:-/data/raw/busan.osm.pbf}
      GRAPHHOPPER_GRAPH_LOCATION: ${GRAPHHOPPER_GRAPH_LOCATION:-/graphhopper/data}
    volumes:
      - ${GRAPHHOPPER_DATA_DIR:-/opt/e102/graphhopper/raw}:/data/raw:ro
      - graphhopper-prod-data:/graphhopper/data
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8990/healthcheck >/dev/null"]
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 3m

  ai:
    build:
      context: ./AI
    restart: unless-stopped
    environment:
      AI_PORT: ${AI_PORT:-5000}
    ports:
      - "127.0.0.1:${AI_PORT:-5000}:5000"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:5000/health', timeout=3).read()\"",
        ]
      interval: 10s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./BE
    restart: unless-stopped
    depends_on:
      ai:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}
      SERVER_PORT: ${SERVER_PORT:-8080}
      DB_URL: ${DB_URL}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      SPRING_JPA_HIBERNATE_DDL_AUTO: ${JPA_DDL_AUTO:-validate}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      REDIS_SSL: ${REDIS_SSL:-true}
      S3_ENDPOINT: ${S3_ENDPOINT}
      S3_BUCKET: ${S3_BUCKET}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      S3_SECRET_KEY: ${S3_SECRET_KEY}
      S3_REGION: ${S3_REGION:-ap-northeast-2}
      GRAPHHOPPER_BASE_URL: ${GRAPHHOPPER_BASE_URL:-http://graphhopper:8989}
      AI_BASE_URL: ${AI_BASE_URL:-http://ai:5000}
      JWT_SECRET: ${JWT_SECRET}
      JWT_ISSUER: ${JWT_ISSUER:-e102}
      JWT_ACCESS_TOKEN_TTL: ${JWT_ACCESS_TOKEN_TTL:-15m}
      JWT_REFRESH_TOKEN_TTL: ${JWT_REFRESH_TOKEN_TTL:-14d}
      JWT_SIGNUP_TOKEN_TTL: ${JWT_SIGNUP_TOKEN_TTL:-10m}
    ports:
      - "127.0.0.1:${SERVER_PORT:-8080}:8080"

volumes:
  graphhopper-prod-data:
