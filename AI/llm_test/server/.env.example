# AI 관련 env 예시
# 실제 값은 저장소 루트의 .env.dev 또는 .env.prod에 넣어 관리한다.

# GMS API 키
GMS_KEY=your_gms_key_here

# 서버 설정
HOST=0.0.0.0
PORT=5000
DEBUG=True # Dev 환경에선 DEBUG=False 필요

# 기본 모델 설정
DEFAULT_MODEL=gemini
