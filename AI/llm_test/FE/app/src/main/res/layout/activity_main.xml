<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <!-- 타이틀 -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="LLM 음성 테스트"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="20dp"/>

    <!-- 모델 선택 스피너 -->
    <Spinner
        android:id="@+id/spinnerModel"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:entries="@array/model_names"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        android:padding="12dp"
        android:background="@android:drawable/spinner_background"/>

    <!-- 결과 표시 영역 -->
    <ScrollView
        android:id="@+id/scrollResult"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/spinnerModel"
        app:layout_constraintBottom_toTopOf="@id/buttonLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="24dp"
        android:background="@android:color/white"
        android:elevation="2dp"
        android:padding="16dp">

        <TextView
            android:id="@+id/tvResult"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@android:color/black"
            android:fontFamily="monospace"
            android:text="테스트를 시작하려면 아래 버튼을 누르세요.\n\n📱 Android STT: 기기 음성인식 사용\n🎙️ Whisper STT: 서버 음성인식 사용"/>
    </ScrollView>

    <!-- 버튼 레이아웃 -->
    <LinearLayout
        android:id="@+id/buttonLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintBottom_toTopOf="@id/switchDarkMode"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="32dp"
        android:gravity="center">

        <!-- Android STT 버튼 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAndroidSTT"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:text="📱\nAndroid\nSTT"
            android:textSize="16sp"
            app:cornerRadius="70dp"
            android:layout_marginEnd="16dp"
            app:strokeWidth="2dp"
            app:strokeColor="?attr/colorPrimary"
            android:contentDescription="Android 음성인식 버튼"/>

        <!-- Whisper STT 버튼 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnWhisperSTT"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:text="🎙️\nWhisper\nSTT"
            android:textSize="16sp"
            app:cornerRadius="70dp"
            android:layout_marginStart="16dp"
            app:strokeWidth="2dp"
            app:strokeColor="?attr/colorSecondary"
            android:contentDescription="Whisper 음성인식 버튼"/>
    </LinearLayout>

    <!-- 다크모드 토글 -->
    <com.google.android.material.switchmaterial.SwitchMaterial
        android:id="@+id/switchDarkMode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="다크 모드"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="24dp"/>

    <!-- 로딩 인디케이터 -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:elevation="4dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
