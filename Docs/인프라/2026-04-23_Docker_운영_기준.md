# Docker 운영 기준

> 작성일: 2026-04-23
> 적용 범위: 루트 compose, `.env.*`, `Makefile`, 서비스별 Dockerfile

---

## 1. 결론

이 프로젝트는 **루트 디렉토리에서 환경별 Docker Compose를 통합 관리**한다.

```text
S14P31E102/
|-- docker-compose.shared.yml
|-- docker-compose.local.yml
|-- docker-compose.dev.yml
|-- docker-compose.prod.yml
|-- .env.local
|-- .env.dev
|-- .env.prod
|-- Makefile
|-- BE/
|   `-- Dockerfile
|-- AI/
|   `-- Dockerfile 또는 작업별 Dockerfile
`-- INF/
    |-- aws/
    |-- jenkins/
    `-- monitoring/
```

`BE/`, `AI/`, `INF/` 아래에 환경별 compose를 각각 두지 않는다.
대신 루트 compose에서 서비스와 profile을 나누고, `Makefile`로 사람이 쓰는 명령을 감싼다.

---

## 2. 왜 루트 compose인가

부산이음길은 백엔드만 단독으로 실행되는 구조가 아니다.

- Spring Boot API
- PostgreSQL/PostGIS
- Redis
- MinIO/S3
- GraphHopper runtime
- AI/ETL 작업
- Jenkins
- SonarQube
- PLG/Grafana

이 서비스들은 네트워크, 포트, env, volume을 공유한다.
따라서 영역별로 compose를 분산하면 같은 설정이 여러 곳에 반복되고, 전체 개발 환경을 한 번에 올리는 기준이 흐려진다.

루트 compose를 기준으로 하면 다음 장점이 있다.

- 전체 실행 진입점이 하나다.
- `.env.local`, `.env.dev`, `.env.prod` 계약을 한 곳에서 관리할 수 있다.
- 서비스 간 네트워크와 volume 이름이 일관된다.
- Jenkins나 운영 배포에서 같은 compose 파일을 참조하기 쉽다.
- 백엔드/AI/운영자가 필요한 일부 서비스만 profile 또는 service name으로 실행할 수 있다.

---

## 3. 책임 분리

| 위치 | 책임 |
|---|---|
| 루트 compose | 환경별 전체 실행 orchestration |
| 루트 shared compose | local/dev에서 재사용하는 공통 dependency definition |
| 루트 `.env.*` | 환경별 변수 계약 |
| `Makefile` | 사람이 실행하는 명령 진입점 |
| `BE/` | Spring Boot 코드, backend Dockerfile |
| `AI/` | AI/ETL 코드, AI 작업용 Dockerfile |
| `INF/aws/` | AWS 기준 설정, SG/ALB/RDS 참고 자산 |
| `INF/jenkins/` | Jenkins pipeline/job 관련 자산 |
| `INF/monitoring/` | PLG/Grafana/Loki/Prometheus 관련 자산 |
| `Docs/인프라/` | 설계안, 운영 기준, runbook |

---

## 4. 환경별 파일 기준

### `docker-compose.shared.yml`

local/dev에서 재사용하는 공통 의존성 정의 파일이다.

현재 범위:

- PostGIS
- Redis
- MinIO
- MinIO bucket init

이 파일은 단독 실행 진입점이 아니라 `local`, `dev` compose가 재사용하는 base 역할을 한다.

### `docker-compose.local.yml`

개인 PC에서 **backend + graphhopper + 공유 의존성**을 함께 올리는 환경이다.

중요:

- 의도된 데이터 방향은 `road_segments` 같은 edge/LineString 기반 구조다.
- 다만 현재 저장소에서 바로 실행 가능한 GraphHopper 런타임 자산은 아직 import 입력 파일과 join file 흐름에 기대고 있다.
- 즉, 현재 `local` compose의 GraphHopper는 **최종 아키텍처 그 자체**라기보다 **로컬 통합 테스트를 위한 과도기 실행 자산**으로 본다.

예상 범위:

- backend WAS
- GraphHopper runtime
- PostGIS
- Redis
- MinIO
- 개인 실험용 override

### `docker-compose.dev.yml`

dev 서버에서 실행하는 통합 개발 환경이다.

예상 범위:

- backend WAS
- GraphHopper runtime
- PostGIS
- Redis
- MinIO
- MinIO bucket init
- 필요 시 AI/ETL runtime
- 필요 시 dev 편의용 부가 서비스

원칙:

- `docker-compose.dev.yml`은 dev 서버에서 DB, 캐시, 스토리지, 경로 엔진을 함께 올리는 기준 파일이다.
- dev 서버의 backend는 compose 내부 service name인 `postgres`, `redis`, `minio`, `graphhopper`를 바라본다.
- dev 서버의 `.env.dev`는 container-network 기준 값을 채운다.
- 개발자 PC에서 dev 서버 자원만 바라보며 WAS를 띄우는 용도는 `make be-dev-up`을 사용한다.
- `make be-dev-up`은 별도 compose 파일을 만들지 않고 `scripts/docker/backend-dev.sh`에서 backend image를 직접 build/run 한다.
- 로컬 PC의 `.env.dev`는 dev 서버 endpoint 또는 SSH/SSM tunnel endpoint를 채운다.

### `docker-compose.prod.yml`

EC2 운영 환경에서 실행할 서비스만 둔다.

포함 후보:

- Spring Boot WAS
- GraphHopper runtime
- Jenkins
- Portainer
- PLG/Grafana/Loki/Prometheus

제외 대상:

- RDS: 운영에서는 AWS managed RDS 사용
- ElastiCache: 운영에서는 AWS managed ElastiCache 사용
- S3: 운영에서는 AWS S3 사용
- GraphHopper import/build heavy job: 운영 WAS 노드에서 직접 수행하지 않음

---

## 5. Profile 기준

compose 파일은 service name 직접 실행과 profile 실행을 모두 허용한다.

공유 의존성 서비스는 `docker-compose.shared.yml`에 둔다.
현재는 `docker-compose.local.yml`과 `docker-compose.dev.yml`이 이를 `extends`로 재사용한다.

공유 의존성 서비스는 profile을 붙이지 않는다.
그래야 `docker compose up -d` 또는 `make dev-up`만으로 dev 서버의 기본 개발 의존성 스택이 올라간다.

profile은 backend, AI/ETL, monitoring처럼 선택적으로 추가되는 애플리케이션/운영 서비스에만 붙인다.
현재 적용 파일에서는 profile보다 파일 분리(`local`, `dev`, `prod`)와 service name 실행이 더 중요한 기준이다.
고정 `container_name`은 사용하지 않는다. Compose project name과 service name이 컨테이너 이름을 만들도록 두어 `local`, `dev`, 임시 검증 실행이 서로 충돌하지 않게 한다.

---

## 6. 실행 명령 기준

직접 실행도 가능하지만, 팀원은 가능하면 `make`를 사용한다.

직접 실행 예시:

```powershell
docker compose --env-file .env.dev -f docker-compose.dev.yml up -d
make be-dev-up
docker compose --env-file .env.local -f docker-compose.local.yml up -d
docker compose --env-file .env.local -f docker-compose.local.yml up -d postgres redis minio minio-init graphhopper backend
```

Makefile 기준:

```bash
make local-config
make local-up
make local-down
make local-logs
make dev-config
make dev-up
make dev-down
make be-dev-config
make be-dev-up
make be-dev-down
make be-dev-logs
make be-local-up
make ai-local-up
make dev-logs
```

---

## 7. 운영 원칙

- compose 파일은 실행 기준이고, 상세 설명은 `Docs/인프라`에 둔다.
- 비밀 값은 compose에 직접 쓰지 않는다.
- `.env.dev`, `.env.prod`, `.env.local`은 git에 올리지 않는다.
- 공유 가능한 placeholder가 필요하면 `.env.example`을 별도로 둔다.
- 운영용 compose에는 managed service인 RDS, ElastiCache, S3를 컨테이너로 올리지 않는다.
- profile은 편의를 위한 선택 실행 단위이고, 실제 서비스 의존성은 명시적으로 관리한다.
- 경로 엔진의 import/build 작업은 runtime compose와 분리한다.

---

## 8. 현재 적용 상태

현재 적용된 초안:

- `docker-compose.local.yml`
  - `docker-compose.shared.yml`의 공유 의존성을 `extends`로 재사용
  - `backend`
  - `graphhopper`
  - `postgres`, `redis`, `minio`, `minio-init`
- `docker-compose.dev.yml`
  - `docker-compose.shared.yml`의 공유 의존성을 `extends`로 재사용
  - `backend`
  - `graphhopper`
  - `postgres`, `redis`, `minio`, `minio-init`
- `scripts/docker/backend-dev.sh`
  - 개발자 PC에서 backend image를 직접 build/run
  - remote dev DB/Redis/S3/GraphHopper endpoint 참조 전제
- `docker-compose.shared.yml`
  - `postgres`
  - `redis`
  - `minio`
  - `minio-init`
- `docker-compose.prod.yml`
  - prod EC2-hosted runtime only 스켈레톤
- `.env.example`
  - 공유 가능한 환경 변수 키 계약
  - 실제 secret 없이 placeholder와 local 기본값만 포함
- `.env.dev`, `.env.prod`
  - 루트에 존재하지만 `.gitignore`의 `.env.*` 규칙으로 무시됨
- `.env.local`
  - 루트에 존재하지만 `.gitignore`의 `.env.*` 규칙으로 무시됨
  - 기본값은 `backend -> postgres/redis/minio/graphhopper` 컨테이너 네트워크 기준으로 맞춘다.
  - `GRAPHHOPPER_DATA_DIR`는 현재 로컬 기본값으로 `.tmp/ieumgil-poc/ieumgil-osm-etl-poc/data/raw`를 참조한다.

검증:

- `docker compose --env-file .env.dev -f docker-compose.dev.yml config` 통과
- `make be-dev-config` 통과
- `docker compose --env-file .env.local -f docker-compose.local.yml config` 통과
- `docker compose --env-file .env.local -f docker-compose.local.yml config --services`
  - `redis`, `postgres`, `graphhopper`, `minio`, `backend`, `minio-init`
- `docker compose --env-file .env.dev -f docker-compose.dev.yml config --services`
  - `graphhopper`, `minio`, `postgres`, `redis`, `backend`, `minio-init`
- Docker Desktop daemon 미실행으로 실제 `up -d` 런타임 검증은 보류

---

## 9. 향후 작업

- AI/ETL service 추가
- ops profile에 SonarQube/PLG 구성 추가
- 운영 compose에서 RDS/ElastiCache/S3는 외부 managed endpoint로만 참조
